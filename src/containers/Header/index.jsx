import React from 'react';
import { Container } from '../../components';

const Header = () => (
    <header className="Header">
        <Container>
            <div className="Header__inner">
                <h2 className="Header__brand">Whether Weather</h2>
            </div>
        </Container>
    </header>
)

export default Header;
